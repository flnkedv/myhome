/** @type {import('next').NextConfig} */
const nextConfig = {

    //distDir: "build", 
    
    //this line will tell the build to create a file with this name
    //ใส่ตอนที่ต้องการสร้างfolder buildแยก เพื่อจะเอาลงserver 
    //แต่ถ้าลงgitก็ไม่ต้อง buildธรรมดา ตัว.nextมันเอง
};

export default nextConfig;
